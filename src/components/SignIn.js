import React from "react";

import './styles/SignIn.css';

const SignIn = ({toggle}) => {
    return <div id="signIn">
        <h1>Sign In!</h1>
        <p>To make posts on FakeNuke, you are required to create an account. We support Google, Github and a <strong>Demo Account</strong>.</p>
    </div>
}

export default SignIn;